# ------------------------------------------------------------------------------
# Worker en ejecución local fuera de Docker
# ------------------------------------------------------------------------------
#
# El worker solo necesita su identidad y la URL del control plane. Kafka,
# ClickHouse y tablas se reciben desde GET /workers/{WORKER_ID}/config.

WORKER_ID=local-worker
WORKER_CONTROL_PLANE_BASE_URL=http://localhost:8000
